image: latest

stages:
  - build
  - test
  - deploy


.before: &before    
  -apt-get update
  -apt-get default-jdk
  -apt-get mvn

default:
  before_script:
    - *before

build:
  stage: build
  script: 
    - cd tp1/usersspringboot
    - mvn package
  artifacts:
    paths:
      - target/demo-0.0.1-SNAPSHOT.jar

