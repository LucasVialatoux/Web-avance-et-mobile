image: latest

default:
  before_script:
    -apt-get update
    -apt-get default-jdk
    -apt-get mvn

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script: 
    - cd tp1/usersspringboot
    - mvn package
  artifacts:
    paths:
      - target/demo-0.0.1-SNAPSHOT.jar

